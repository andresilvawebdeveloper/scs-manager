import "./chunk-VUNV25KB.js";

// node_modules/react-onesignal/dist/index.js
var ONESIGNAL_SDK_ID = "onesignal-sdk";
var DEFAULT_SCRIPT_SRC = "https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.page.js";
var isOneSignalInitialized = false;
var isOneSignalScriptFailed = false;
var pendingInitReject;
if (typeof window !== "undefined") window.OneSignalDeferred = window.OneSignalDeferred || [];
function handleOnError() {
  isOneSignalScriptFailed = true;
  pendingInitReject == null ? void 0 : pendingInitReject(new Error("OneSignal script failed to load."));
  pendingInitReject = void 0;
}
function addSDKScript(scriptSrc) {
  if (document.getElementById(ONESIGNAL_SDK_ID)) return;
  const script = document.createElement("script");
  script.id = ONESIGNAL_SDK_ID;
  script.defer = true;
  script.src = scriptSrc || DEFAULT_SCRIPT_SRC;
  script.onerror = () => {
    handleOnError();
  };
  document.head.appendChild(script);
}
function isPushNotificationsSupported() {
  return supportsVapidPush() || supportsSafariPush();
}
function isMacOSSafariInIframe() {
  return window.top !== window && navigator.vendor === "Apple Computer, Inc." && navigator.platform === "MacIntel";
}
function supportsSafariPush() {
  return window.safari && typeof window.safari.pushNotification !== "undefined" || isMacOSSafariInIframe();
}
function supportsVapidPush() {
  return typeof PushSubscriptionOptions !== "undefined" && PushSubscriptionOptions.prototype.hasOwnProperty("applicationServerKey");
}
var isPushSupported = () => {
  return isPushNotificationsSupported();
};
var init = (options) => {
  var _a;
  if (isOneSignalInitialized) return Promise.reject(`OneSignal is already initialized.`);
  if (!options || !options.appId) return Promise.reject("You need to provide your OneSignal appId.");
  if (!document) return Promise.reject(`Document is not defined.`);
  if (!isPushNotificationsSupported()) return Promise.reject(new Error("This browser does not support Web Push notifications."));
  if (((_a = options.welcomeNotification) == null ? void 0 : _a.disabled) !== void 0) options.welcomeNotification.disable = options.welcomeNotification.disabled;
  addSDKScript(options.scriptSrc);
  return new Promise((resolve, reject) => {
    var _a2;
    pendingInitReject = reject;
    (_a2 = window.OneSignalDeferred) == null ? void 0 : _a2.push((OneSignal2) => {
      OneSignal2.init(options).then(() => {
        isOneSignalInitialized = true;
        pendingInitReject = void 0;
        resolve();
      }).catch((err) => {
        pendingInitReject = void 0;
        reject(err);
      });
    });
  });
};
function oneSignalLogin(externalId, jwtToken) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.login(externalId, jwtToken).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function oneSignalLogout() {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.logout().then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function oneSignalSetConsentGiven(consent) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.setConsentGiven(consent).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function oneSignalSetConsentRequired(requiresConsent) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.setConsentRequired(requiresConsent).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function slidedownPromptPush(options) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Slidedown.promptPush(options).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function slidedownPromptPushCategories(options) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Slidedown.promptPushCategories(options).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function slidedownPromptSms(options) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Slidedown.promptSms(options).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function slidedownPromptEmail(options) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Slidedown.promptEmail(options).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function slidedownPromptSmsAndEmail(options) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Slidedown.promptSmsAndEmail(options).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function slidedownAddEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.Slidedown.addEventListener(event, listener);
  });
}
function slidedownRemoveEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.Slidedown.removeEventListener(event, listener);
  });
}
function notificationsSetDefaultUrl(url) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Notifications.setDefaultUrl(url).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function notificationsSetDefaultTitle(title) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Notifications.setDefaultTitle(title).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function notificationsRequestPermission() {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Notifications.requestPermission().then((result) => resolve(result)).catch((error) => reject(error));
    });
  });
}
function notificationsAddEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.Notifications.addEventListener(event, listener);
  });
}
function notificationsRemoveEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.Notifications.removeEventListener(event, listener);
  });
}
function sessionSendOutcome(outcomeName, outcomeWeight) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Session.sendOutcome(outcomeName, outcomeWeight).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function sessionSendUniqueOutcome(outcomeName) {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.Session.sendUniqueOutcome(outcomeName).then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function userAddAlias(label, id) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addAlias(label, id);
  });
}
function userAddAliases(aliases) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addAliases(aliases);
  });
}
function userRemoveAlias(label) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeAlias(label);
  });
}
function userRemoveAliases(labels) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeAliases(labels);
  });
}
function userAddEmail(email) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addEmail(email);
  });
}
function userRemoveEmail(email) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeEmail(email);
  });
}
function userAddSms(smsNumber) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addSms(smsNumber);
  });
}
function userRemoveSms(smsNumber) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeSms(smsNumber);
  });
}
function userAddTag(key, value) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addTag(key, value);
  });
}
function userAddTags(tags) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addTags(tags);
  });
}
function userRemoveTag(key) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeTag(key);
  });
}
function userRemoveTags(keys) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeTags(keys);
  });
}
async function userGetTags() {
  var _a;
  let retVal;
  await ((_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    retVal = OneSignal2.User.getTags();
  }));
  return retVal;
}
function userAddEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.addEventListener(event, listener);
  });
}
function userRemoveEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.removeEventListener(event, listener);
  });
}
function userSetLanguage(language) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.setLanguage(language);
  });
}
async function userGetLanguage() {
  var _a;
  let retVal;
  await ((_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    retVal = OneSignal2.User.getLanguage();
  }));
  return retVal;
}
function userTrackEvent(name, properties) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.trackEvent(name, properties);
  });
}
function pushSubscriptionOptIn() {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.User.PushSubscription.optIn().then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function pushSubscriptionOptOut() {
  return new Promise((resolve, reject) => {
    var _a;
    if (isOneSignalScriptFailed) {
      reject(new Error("OneSignal script failed to load."));
      return;
    }
    (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
      OneSignal2.User.PushSubscription.optOut().then(() => resolve()).catch((error) => reject(error));
    });
  });
}
function pushSubscriptionAddEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.PushSubscription.addEventListener(event, listener);
  });
}
function pushSubscriptionRemoveEventListener(event, listener) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.User.PushSubscription.removeEventListener(event, listener);
  });
}
function debugSetLogLevel(logLevel) {
  var _a;
  (_a = window.OneSignalDeferred) == null ? void 0 : _a.push((OneSignal2) => {
    OneSignal2.Debug.setLogLevel(logLevel);
  });
}
var OneSignal = {
  login: oneSignalLogin,
  logout: oneSignalLogout,
  init,
  setConsentGiven: oneSignalSetConsentGiven,
  setConsentRequired: oneSignalSetConsentRequired,
  Slidedown: {
    promptPush: slidedownPromptPush,
    promptPushCategories: slidedownPromptPushCategories,
    promptSms: slidedownPromptSms,
    promptEmail: slidedownPromptEmail,
    promptSmsAndEmail: slidedownPromptSmsAndEmail,
    addEventListener: slidedownAddEventListener,
    removeEventListener: slidedownRemoveEventListener
  },
  Notifications: {
    get permissionNative() {
      var _a, _b;
      return ((_b = (_a = window.OneSignal) == null ? void 0 : _a.Notifications) == null ? void 0 : _b.permissionNative) ?? "default";
    },
    get permission() {
      var _a, _b;
      return ((_b = (_a = window.OneSignal) == null ? void 0 : _a.Notifications) == null ? void 0 : _b.permission) ?? false;
    },
    setDefaultUrl: notificationsSetDefaultUrl,
    setDefaultTitle: notificationsSetDefaultTitle,
    isPushSupported,
    requestPermission: notificationsRequestPermission,
    addEventListener: notificationsAddEventListener,
    removeEventListener: notificationsRemoveEventListener
  },
  Session: {
    sendOutcome: sessionSendOutcome,
    sendUniqueOutcome: sessionSendUniqueOutcome
  },
  User: {
    get onesignalId() {
      var _a, _b;
      return (_b = (_a = window.OneSignal) == null ? void 0 : _a.User) == null ? void 0 : _b.onesignalId;
    },
    get externalId() {
      var _a, _b;
      return (_b = (_a = window.OneSignal) == null ? void 0 : _a.User) == null ? void 0 : _b.externalId;
    },
    addAlias: userAddAlias,
    addAliases: userAddAliases,
    removeAlias: userRemoveAlias,
    removeAliases: userRemoveAliases,
    addEmail: userAddEmail,
    removeEmail: userRemoveEmail,
    addSms: userAddSms,
    removeSms: userRemoveSms,
    addTag: userAddTag,
    addTags: userAddTags,
    removeTag: userRemoveTag,
    removeTags: userRemoveTags,
    getTags: userGetTags,
    addEventListener: userAddEventListener,
    removeEventListener: userRemoveEventListener,
    setLanguage: userSetLanguage,
    getLanguage: userGetLanguage,
    trackEvent: userTrackEvent,
    PushSubscription: {
      get id() {
        var _a, _b, _c;
        return (_c = (_b = (_a = window.OneSignal) == null ? void 0 : _a.User) == null ? void 0 : _b.PushSubscription) == null ? void 0 : _c.id;
      },
      get token() {
        var _a, _b, _c;
        return (_c = (_b = (_a = window.OneSignal) == null ? void 0 : _a.User) == null ? void 0 : _b.PushSubscription) == null ? void 0 : _c.token;
      },
      get optedIn() {
        var _a, _b, _c;
        return (_c = (_b = (_a = window.OneSignal) == null ? void 0 : _a.User) == null ? void 0 : _b.PushSubscription) == null ? void 0 : _c.optedIn;
      },
      optIn: pushSubscriptionOptIn,
      optOut: pushSubscriptionOptOut,
      addEventListener: pushSubscriptionAddEventListener,
      removeEventListener: pushSubscriptionRemoveEventListener
    }
  },
  Debug: { setLogLevel: debugSetLogLevel }
};
export {
  OneSignal as default
};
//# sourceMappingURL=react-onesignal.js.map
